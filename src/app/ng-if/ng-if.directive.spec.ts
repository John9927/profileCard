import { NgIfDirective } from './ng-if.directive';

describe('NgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfDirective();
    expect(directive).toBeTruthy();
  });
});
