import { ContainerCardDirective } from './container-card.directive';

describe('ContainerCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerCardDirective();
    expect(directive).toBeTruthy();
  });
});
