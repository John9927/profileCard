import { NgstyleDirective } from './ngstyle.directive';

describe('NgstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgstyleDirective();
    expect(directive).toBeTruthy();
  });
});
